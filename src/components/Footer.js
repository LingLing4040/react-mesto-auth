import React from 'react';

function Footer() {
    return (
        <footer>
            <p className='footer'>&copy; {new Date().getFullYear()} Mesto Russia</p>
        </footer>
    );
}

export default Footer;
